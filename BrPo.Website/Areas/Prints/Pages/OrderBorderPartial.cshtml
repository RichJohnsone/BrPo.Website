<div class="container p-0">
    <div class="form-group row mb-0">
        <label for="height" class="col-sm-4 col-form-label">Border</label>
        <div class="col-sm-4">
            <input type="number" step="1"
                class="form-control integer-control-js" 
                id="border" 
                placeholder="Border" 
                data-val="true"
                data-val-number="The Border field must be an integer between 1 and 100" 
                min="0" 
                max="100" 
                title="Enter a number to create a border within the overall print size"
                name="border" />
        </div>
        <div class="col-sm-4">
            <div class="text mt-2">mm</div>
        </div>
    </div>
</div>